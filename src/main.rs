//! This is a library project. The main binaries are in src/bin/

fn main() {
    println!("This is a library project!");
    println!("Run the crawler with: cargo run --bin crawler");
    println!("Run the search server with: cargo run --bin search-server");
}
